version: '2'
services:

  db:
    image: postgres
    volumes:
      - ./db:/var/lib/postgresql

  web:
    build: ./web
    command: python manage.py runserver 0.0.0.0:8010
    volumes:
      - ./web:/code
    ports:
      - "8010:8010"
    depends_on:
      - db
